import "./style.css";

// This should be an object stored in the database that has:
// title, description, upvote? downvote?, id?
function Post(props) {
    return (
        <div>
            Post
        </div>
    )
}

export default Post;